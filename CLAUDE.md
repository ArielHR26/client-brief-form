# client-brief-form — הקשר פרויקט

טופס אפיון (brief) לפרויקטים של אנליסט: הלקוח הפנימי בוחר סוג פרויקט בראש הטופס ומקבל
מערך שאלות עצמאי אחד שמתאים לו בלבד. הטופס נבנה כתבנית לכל אנליסט, לא כטופס חד-פעמי.

## Stack

HTML/CSS/JS סטטי בקובץ אחד. אין build, אין npm, אין backend.

## Layout

- `index.html` — הטופס עצמו: בורר סוג הפרויקט בראש, ואחריו מערך השאלות של אותו סוג.
- `setup-guide.html` — מדריך הפעלה עם קישורים ישירים לקבצים ומסלול העתקה בלחיצה.
- `SETUP.md` — אותו מדריך כטקסט.

## Run / build / test

לפתוח את `index.html` בדפדפן. אין טסטים.

## איך זה עולה לאוויר

**לא ידוע — לאמת.** אין `netlify.toml`, אין workflow, אין קונפיג פריסה בריפו.
נכון לעכשיו זה נראה כמו קובץ שמופץ/נפתח מקומית ולא כאתר חי.

## Git

`origin` = `https://github.com/ArielHR26/client-brief-form.git`, ענף `main`.
זהות עבודה (ArielHR26) לפי הנתיב תחת `Projects\עבודה\`. הודעות קומיט באנגלית, שורה אחת.

## מוסכמות ומלכודות

- אין `.gitignore` בריפו הזה — לפני קומיט לבדוק שלא נגררו קבצים מקומיים.
- העיצוב הנוכחי הוא תוצאה של קיצוץ מכוון (`8c2fe61`): מערך שאלות אחד לכל סוג פרויקט,
  בלי הסתעפויות. לא להחזיר שאלות משותפות/כלליות בלי בקשה מפורשת.

## נתונים רגישים

תשובות אמיתיות של לקוחות פנימיים לא נכנסות לריפו.
