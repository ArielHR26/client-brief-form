# איך מקימים טופס אפיון משלכם

הטופס הזה נועד להיות מועתק. כל אנליסט מקים עותק עם הלוגו שלו והמייל שלו, מקבל כתובת אינטרנט
משלו, ושולח אותה ליחידה שפנתה אליו. התשובות מגיעות ישר לתיבת המייל שלו.

זמן ההקמה בפעם הראשונה: בערך 20 דקות. אחר כך אפשר לשכוח מזה.

צריך חשבון GitHub. אם אין לכם, זה השלב הראשון. בהמשך המדריך יש סעיף שמסביר מה בדיוק
פומבי כאן ומה לא, כי זו השאלה הראשונה שכולם שואלים.

---

## שלב 1: חשבון GitHub וריפו חדש

1. נכנסים ל-[github.com/signup](https://github.com/signup) ופותחים חשבון. הרשמה חינמית.
2. אחרי ההרשמה לוחצים על **New** ויוצרים ריפו חדש (repository). קוראים לו למשל `project-brief`.
3. חשוב: משאירים אותו **Public**. GitHub Pages לא עובד על ריפו פרטי בחשבון חינמי.

## שלב 2: מעלים את הקבצים

1. מקבלים מאורי את `index.html`.
2. בריפו החדש: **Add file → Upload files**, גוררים את `index.html`, ולוחצים **Commit changes**.
3. אם יש לכם לוגו, מעלים גם אותו באותה דרך. שמרו אותו בשם `logo.png`, PNG עם רקע שקוף,
   ברוחב 400 פיקסלים בערך. תמונה גדולה מזה סתם מאטה את הטופס.

## שלב 3: מדליקים את האתר

1. בריפו: **Settings → Pages**.
2. תחת **Source** בוחרים **Deploy from a branch**, ותחת **Branch** בוחרים `main` ואת התיקייה `/ (root)`.
3. **Save**. אחרי דקה או שתיים הכתובת תופיע באותו עמוד, בצורה
   `https://<שם-המשתמש-שלכם>.github.io/project-brief/`.

זו הכתובת שתשלחו ליחידות. שמרו אותה.

## שלב 4: חשבון EmailJS

EmailJS הוא השירות שלוקח את מה שמולא בטופס ושולח לכם אותו במייל. חינמי עד 200 מיילים בחודש.

1. נרשמים ב-[emailjs.com](https://www.emailjs.com).
2. **Email Services → Add New Service**, בוחרים Gmail (או Outlook), ומחברים את חשבון המייל
   שממנו יישלחו ההודעות. בסוף תקבלו **Service ID**.
3. **Email Templates → Create New Template**. כאן שני שדות קובעים הכל:

   | שדה בתבנית | מה לכתוב בו |
   |---|---|
   | To Email | `{{to_email}}` |
   | Reply To | `{{from_email}}` |
   | Subject | `אפיון חדש מ-{{from_unit}}` |
   | Content | `{{message}}` |

   `{{to_email}}` הוא הסיבה שהתבנית עובדת לכל אחד. אם תכתבו שם כתובת מייל קשיחה, הטופס ישלח
   לשם ולא לכם, ואי אפשר יהיה להבין למה. `{{from_email}}` בשדה Reply To אומר שכשתלחצו "השב"
   על המייל, התשובה תגיע לאדם שמילא את הטופס ולא לעצמכם.

   שומרים, ומקבלים **Template ID**.
4. **Account → General**: משם לוקחים את ה-**Public Key**.
5. **Account → Security**: מדליקים **Allow list** ומוסיפים את הדומיין `<שם-המשתמש>.github.io`.
   בלי זה, מי שמעתיק את הקוד שלכם יכול לשלוח מיילים על חשבון המנה שלכם.

## שלב 5: ממלאים את בלוק ההגדרות

פותחים את `index.html` בריפו ולוחצים על העיפרון (Edit). קרוב לסוף הקובץ יש בלוק אחד מסומן.
זה כל מה שצריך לגעת בו:

```js
const CONFIG = {
  ownerName:  "דנה כהן",
  ownerRole:  "אנליסטית BI",
  ownerUnit:  "אגף משאבי אנוש",
  ownerEmail: "dana@ariel.ac.il",

  logo: "logo.png",

  emailjs: {
    publicKey:  "המפתח מהשלב הקודם",
    serviceId:  "service_xxxxxxx",
    templateId: "template_xxxxxxx"
  },

  sourceSystems: [
    "אקסל / Google Sheets",
    "מערכת ניהול סטודנטים",
    "מערכת כספים",
    "מערכת משאבי אנוש",
    "Moodle",
    "מסד נתונים",
    "טופס או סקר",
    "הקלדה ידנית"
  ]
};
```

שני דברים ששווה להתאים:

- `logo` נשאר ריק (`""`) אם אין לכם קובץ לוגו. אז יוצג השם שלכם כטקסט, והטופס נראה תקין.
- `sourceSystems` היא הרשימה שמופיעה בשאלה "מאיפה הנתונים מגיעים?". ערכו אותה למערכות
  שאתם באמת עובדים איתן. אנליסט באגף כספים ואנליסט במדור בחינות עובדים מול מערכות אחרות
  לגמרי, וכשהרשימה מדויקת הפונים מסמנים במקום לכתוב.

**Commit changes**, ואחרי דקה השינוי יהיה באוויר.

## שלב 6: בודקים שזה עובד

נכנסים לכתובת שלכם, ממלאים את הטופס בנתונים מומצאים, ושולחים. המייל צריך להגיע תוך שניות.

אם לא הגיע: פתחו את הכתובת בכרום, לחצו F12, ותסתכלו על הלשונית Console. שגיאה של EmailJS
תופיע שם עם הסבר. ברוב המקרים הבעיה היא אחת מהשלוש: מפתח שהועתק עם רווח מיותר, שדה To Email
שאינו `{{to_email}}`, או Allow list שמוגדר לדומיין אחר.

---

## אבטחת מידע: מה פומבי ומה לא

הריפו פומבי, וזו בדרך כלל השאלה הראשונה. אז בפירוט:

**מה כל אחד יכול לראות:** את קובץ ה-HTML, את הלוגו, ואת מפתח ה-EmailJS הפומבי. המפתח הזה
נקרא Public Key כי הוא נועד לשבת בקוד של דף אינטרנט. הוא מזהה את החשבון, הוא לא נותן גישה
אליו, ואי אפשר לקרוא איתו שום מייל ששלחתם או קיבלתם.

**מה אף אחד לא יכול לראות:** את התשובות. אף מילוי של הטופס לא נשמר בריפו ולא ב-GitHub.
מה שמולא נשלח דרך EmailJS ישר לתיבה שלכם ונעלם מהדף. אין מסד נתונים, אין ארכיון, אין לוג.

**מה כן צריך לעשות:** להדליק את ה-Allow list בשלב 4.5. זה מה שמונע ממישהו לקחת את הקוד שלכם
ולשלוח מיילים על חשבון המנה החודשית שלכם.

**מה לא לעשות:**

- לא להעלות לריפו קבצי נתונים אמיתיים. שום אקסל של סטודנטים, שום ייצוא ממערכת, שום `.db`.
  הריפו מכיל קובץ טופס ולוגו, וזה הכל.
- לא להשתמש בטופס לאיסוף מידע אישי. הטופס שואל את היחידה מה הצורך שלה, לא מבקש ממנה נתונים.
  אם פונה מתחיל להדביק תעודות זהות או ציונים בשדה טקסט, עצרו אותו והעבירו את זה לערוץ מאובטח.
  המילוי עובר בדרכו אליכם דרך שרת של EmailJS, וזה בסדר לתיאור צורך, לא לנתונים.

## שאלות שחוזרות

**אפשר להוסיף או לשנות שאלות?**
כן, זה HTML רגיל. רק שימו לב לדבר אחד: לכל שדה יש `name`, ובתוך הפונקציה `buildSummary` יש
מילון שמתרגם כל `name` לכיתוב בעברית. שדה שנוסיף בלי לרשום אותו במילון פשוט לא יופיע במייל,
בלי שום הודעת שגיאה. זו הטעות הכי נפוצה בקובץ הזה.

**מה קורה אם עוברים את 200 המיילים בחודש?**
EmailJS מפסיק לשלוח עד תחילת החודש הבא. לטופס אפיון שנשלח לכמה יחידות בשנה זה רחוק מלהיות
בעיה. אם אתם מתקרבים למגבלה, כנראה שהטופס משמש למשהו אחר ממה שהוא נבנה בשבילו.

**אפשר לשנות את העיצוב?**
כן. כל ה-CSS יושב בראש הקובץ, והצבעים מוגדרים כמשתנים תחת `:root`. שינוי של `--color-accent`
מחליף את צבע ההדגשה בכל הטופס.

**איך מקבלים את כל התשובות במקום אחד ולא כמיילים בודדים?**
אין תשובה טובה לזה בגרסה הזאת. אם תגיעו למצב שהטופס נשלח לעשרות יחידות ואי אפשר לעקוב
במייל, דברו עם אורי, כי זה כבר צריך פתרון אחר.
